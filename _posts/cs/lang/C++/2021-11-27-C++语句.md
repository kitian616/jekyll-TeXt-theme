---
tags: C++
categories:
  CS C/C++
---

```c++
#include <iostream>

using namespace std;

/**
 * 表达式语句
 *      a = a + 3;
 *      z = x < y ? x : y;
 * 函数调用语句
 *      printBinary(12);
 * 输入输出语句
 *      cout, cin
 * 选择语句
 *      if 语句
 *      switch 语句
 * 循环语句
 *      while 语句
 *      do-while 语句
 *      for 语句
 * 控制转移语句
 *      continue 语句
 *      break 语句
 *      goto 语句
 *      return 语句
 * 辅助语句
 *      空语句
 *          ;
 *      复合语句（当一条语句看）
 *          {
 *              a = a + 3;
 *              z = y = x = 0;
 *              z = x < y ? x : y;
 *          }
 * 顺序结构
 *      程序控制按语句的先后顺序执行，顺序排列的语句即为顺序结构
 */

int main() {
}
```