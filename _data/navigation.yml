header:
  - titles:
      # @start locale config
      en      : &EN       Quick Guide
      # @end locale config
    url: /quick-guide.html
  - titles:
      # @start locale config
      en      : &EN       Submission process
      # @end locale config
    url: /submission.html

  - titles:
      # @start locale config
      en      : &EN       Request data process
      # @end locale config
    url: /request-dataset.html

  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about.html
  
  - titles:
      # @start locale config
      en      : &EN       Legal
      # @end locale config
    url: /legal.html


