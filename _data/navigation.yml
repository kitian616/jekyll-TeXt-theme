header:

  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /

  - titles:
      # @start locale config
      en      : &EN       EX 280
      # @end locale config
    url: /ex280

  - titles:
      # @start locale config
      en      : &EN       Categories
      # @end locale config
    url: /categories

  - titles:
      # @start locale config
      en      : &EN       Archive
      # @end locale config
    url: /archive

  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about

  